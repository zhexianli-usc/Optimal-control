# FEniCSx (DOLFINx) and dependencies for Optimal_control_fenicsx.py
# Install via conda (recommended, especially on Windows):
#
#   conda create -n fenicsx-env
#   conda activate fenicsx-env
#   conda install -c conda-forge fenics-dolfinx pyvista scipy
#   (On Linux/macOS you can add mpich; on Windows omit it.)
#   pip install scipy
#
# Or on Debian/Ubuntu:
#   sudo apt install fenicsx
#   pip install scipy
#
# Then run (single process):
#   python Optimal_control_fenicsx.py
# or with MPI:
#   mpiexec -n 1 python Optimal_control_fenicsx.py

scipy>=1.7.0
mpi4py>=3.0
